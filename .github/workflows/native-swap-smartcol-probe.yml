name: Native swap SmartCol callback probe

on:
  push:
    branches: [audit/absorb-gas-dos]
    paths:
      - 'test/foundry/security/nativeSwapSmartColOracleProbe.t.sol'
      - '.github/workflows/native-swap-smartcol-probe.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit/absorb-gas-dos
          submodules: recursive
      - uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - run: npm ci
      - name: Run fork callback probe
        shell: bash
        run: |
          set -o pipefail
          mkdir -p automation
          forge test --match-path test/foundry/security/nativeSwapSmartColOracleProbe.t.sol -vvv 2>&1 | tee automation/native-swap-smartcol-probe.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: native-swap-smartcol-probe
          path: automation/native-swap-smartcol-probe.log
