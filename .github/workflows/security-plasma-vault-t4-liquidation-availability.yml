name: Security Plasma Vault T4 liquidation availability

on:
  push:
    branches:
      - audit/plasma-t4-oracle-drift-20260806
    paths:
      - 'test/security/PlasmaVaultT4LiquidationAvailability.t.sol'
      - '.github/workflows/security-plasma-vault-t4-liquidation-availability.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  liquidation-availability:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PLASMA_RPC_URL: https://rpc.plasma.to
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable

      - name: Resolve fresh Plasma fork block
        shell: bash
        run: |
          echo "PLASMA_FORK_BLOCK=$(cast block-number --rpc-url "$PLASMA_RPC_URL")" >> "$GITHUB_ENV"

      - name: Prepare isolated harness
        shell: bash
        run: |
          rm -rf audit/t4-liquidation-availability-poc
          mkdir -p audit/t4-liquidation-availability-poc/test audit/t4-liquidation-availability-poc/lib evidence-plasma-vault-t4-liquidation-availability
          cp test/security/PlasmaVaultT4LiquidationAvailability.t.sol audit/t4-liquidation-availability-poc/test/
          cp -R lib/forge-std audit/t4-liquidation-availability-poc/lib/forge-std

      - name: Compare liquidation availability
        shell: bash
        run: |
          set -o pipefail
          forge test \
            --root audit/t4-liquidation-availability-poc \
            --via-ir \
            --match-path test/PlasmaVaultT4LiquidationAvailability.t.sol \
            -vvvv 2>&1 | tee evidence-plasma-vault-t4-liquidation-availability/availability.log

      - name: Extract evidence
        if: always()
        shell: bash
        run: |
          grep -E "(AvailabilityState|ManipulationSummary)\(|Suite result|Failing tests|Error:|Compiler run failed" \
            evidence-plasma-vault-t4-liquidation-availability/availability.log \
            | tee evidence-plasma-vault-t4-liquidation-availability/summary.log || true

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plasma-vault-t4-liquidation-availability
          path: evidence-plasma-vault-t4-liquidation-availability/
          if-no-files-found: error
          retention-days: 30
