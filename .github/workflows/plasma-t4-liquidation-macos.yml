name: Plasma T4 liquidation macOS

on:
  push:
    branches:
      - audit/plasma-t4-liquidation-20260806
    paths:
      - '.github/workflows/plasma-t4-liquidation-macos.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  liquidation-macos:
    runs-on: macos-14
    timeout-minutes: 35
    env:
      PLASMA_RPC_URL: https://rpc.plasma.to
      PLASMA_FORK_BLOCK: 29082579
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable
      - name: Prepare isolated probe
        shell: bash
        run: |
          rm -rf audit/liquidation-macos
          mkdir -p audit/liquidation-macos/test audit/liquidation-macos/lib evidence-liquidation-macos
          cp test/security/PlasmaVaultT4PureDexDebtCloseMatrixV2.t.sol audit/liquidation-macos/test/
          cp test/security/PlasmaVaultT4LiquidationSurfaceProbe.t.sol audit/liquidation-macos/test/
          cp -R lib/forge-std audit/liquidation-macos/lib/forge-std
      - name: Run liquidation probe
        shell: bash
        run: |
          set -o pipefail
          forge test --root audit/liquidation-macos --via-ir \
            --match-contract PlasmaVaultT4LiquidationSurfaceProbeTest \
            --match-test test_liquidationSurfaceUnderPureDexPerturb -vvvv 2>&1 \
            | tee evidence-liquidation-macos/liquidation.log
      - name: Extract evidence
        if: always()
        shell: bash
        run: |
          grep -E '(LiquidationProbeSurface|LiquidationSimulation|LiquidationPerturbControl|LiquidationInteraction)\(|Suite result|Failing tests|Error:|revert' \
            evidence-liquidation-macos/liquidation.log | tee evidence-liquidation-macos/summary.log || true
          echo "$PLASMA_FORK_BLOCK" > evidence-liquidation-macos/block.txt
          git rev-parse HEAD > evidence-liquidation-macos/commit.txt
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plasma-t4-liquidation-macos
          path: evidence-liquidation-macos/
          if-no-files-found: error
          retention-days: 30
