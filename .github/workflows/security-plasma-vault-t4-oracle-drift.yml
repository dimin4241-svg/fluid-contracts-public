name: Security Plasma Vault T4 oracle drift

on:
  push:
    branches:
      - audit/plasma-t4-oracle-drift-20260806
    paths:
      - 'test/security/PlasmaVaultT4OracleDrift.t.sol'
      - '.github/workflows/security-plasma-vault-t4-oracle-drift.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  oracle-drift:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PLASMA_RPC_URL: https://rpc.plasma.to
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable

      - name: Resolve fresh Plasma fork block
        shell: bash
        run: |
          echo "PLASMA_FORK_BLOCK=$(cast block-number --rpc-url "$PLASMA_RPC_URL")" >> "$GITHUB_ENV"

      - name: Prepare isolated oracle drift harness
        shell: bash
        run: |
          rm -rf audit/t4-oracle-drift-poc
          mkdir -p audit/t4-oracle-drift-poc/test audit/t4-oracle-drift-poc/lib evidence-plasma-vault-t4-oracle-drift
          cp test/security/PlasmaVaultT4OracleDrift.t.sol audit/t4-oracle-drift-poc/test/
          cp -R lib/forge-std audit/t4-oracle-drift-poc/lib/forge-std

      - name: Probe same-block T4 oracle drift
        shell: bash
        run: |
          set -o pipefail
          forge test \
            --root audit/t4-oracle-drift-poc \
            --via-ir \
            --match-path test/PlasmaVaultT4OracleDrift.t.sol \
            -vvvv 2>&1 | tee evidence-plasma-vault-t4-oracle-drift/oracle-drift.log

      - name: Extract drift evidence
        if: always()
        shell: bash
        run: |
          grep -E "(DriftTarget|OracleDriftResult)\(|Suite result|Failing tests|Error:|Compiler run failed" \
            evidence-plasma-vault-t4-oracle-drift/oracle-drift.log \
            | tee evidence-plasma-vault-t4-oracle-drift/summary.log || true

      - name: Upload drift evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plasma-vault-t4-oracle-drift
          path: evidence-plasma-vault-t4-oracle-drift/
          if-no-files-found: error
          retention-days: 30
