name: fSL feeOrReward check 2026-08-22 07:45Z

on:
  push:
    branches:
      - audit/read-only-dex-fees-20260805
    paths:
      - .github/workflows/fsl-check-20260822-0745Z.yml
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit/read-only-dex-fees-20260805
      - name: Check all 21 fSL feeOrReward values
        run: |
          python3 -m pip install requests
          python3 scripts/security/check_fsl_fee_or_reward.py
