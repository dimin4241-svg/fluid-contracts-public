name: Security Plasma Vault T4 oracle borrow capacity

on:
  push:
    branches:
      - audit/plasma-smart-lending-execution-20260806
    paths:
      - 'test/security/PlasmaVaultT4OracleBorrowCapacity.t.sol'
      - '.github/workflows/security-plasma-vault-t4-oracle-borrow-capacity.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  capacity:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PLASMA_RPC_URL: https://rpc.plasma.to
      PLASMA_FORK_BLOCK: 29082579
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable
      - name: Prepare isolated harness
        shell: bash
        run: |
          rm -rf audit/t4-oracle-capacity
          mkdir -p audit/t4-oracle-capacity/test audit/t4-oracle-capacity/lib evidence-plasma-vault-t4-oracle-capacity
          cp test/security/PlasmaVaultT4OracleBorrowCapacity.t.sol audit/t4-oracle-capacity/test/
          cp -R lib/forge-std audit/t4-oracle-capacity/lib/forge-std
      - name: Run capacity probe
        shell: bash
        run: |
          set -o pipefail
          forge test --root audit/t4-oracle-capacity --via-ir \
            --match-path test/PlasmaVaultT4OracleBorrowCapacity.t.sol -vvvv 2>&1 \
            | tee evidence-plasma-vault-t4-oracle-capacity/capacity.log
      - name: Extract evidence
        if: always()
        shell: bash
        run: |
          grep -E "CapacityResult\(|Suite result|Failing tests|Error:|revert" \
            evidence-plasma-vault-t4-oracle-capacity/capacity.log \
            | tee evidence-plasma-vault-t4-oracle-capacity/summary.log || true
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plasma-vault-t4-oracle-borrow-capacity
          path: evidence-plasma-vault-t4-oracle-capacity/
          if-no-files-found: error
          retention-days: 30
