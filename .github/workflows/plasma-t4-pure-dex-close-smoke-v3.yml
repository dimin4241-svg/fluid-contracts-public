name: Plasma T4 pure DEX close smoke V3

on:
  push:
    branches:
      - audit/plasma-t4-exact-debt-controls-20260806
    paths:
      - 'test/security/PlasmaVaultT4PureDexDebtCloseSmokeV3.t.sol'
      - '.github/workflows/plasma-t4-pure-dex-close-smoke-v3.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fork-test:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      PLASMA_RPC_URL: https://rpc.plasma.to
      PLASMA_FORK_BLOCK: 29082579
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable
      - name: Prepare isolated test
        shell: bash
        run: |
          rm -rf audit/t4-pure-dex-smoke-v3
          mkdir -p audit/t4-pure-dex-smoke-v3/test audit/t4-pure-dex-smoke-v3/lib evidence-v3
          cp test/security/PlasmaVaultT4PureDexDebtCloseSmokeV3.t.sol audit/t4-pure-dex-smoke-v3/test/
          cp -R lib/forge-std audit/t4-pure-dex-smoke-v3/lib/forge-std
      - name: Run pinned fork test
        shell: bash
        run: |
          set -o pipefail
          forge test --root audit/t4-pure-dex-smoke-v3 --via-ir \
            --match-path test/PlasmaVaultT4PureDexDebtCloseSmokeV3.t.sol -vvvv 2>&1 \
            | tee evidence-v3/smoke-v3.log
      - name: Summarize
        if: always()
        shell: bash
        run: |
          grep -E '(SurfaceV3|BaselineV3|PerturbV3|InteractionV3)\(|Suite result|Failing tests|Error:|revert' \
            evidence-v3/smoke-v3.log | tee evidence-v3/summary.log || true
          echo "$PLASMA_FORK_BLOCK" > evidence-v3/block.txt
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plasma-t4-pure-dex-close-smoke-v3
          path: evidence-v3/
          if-no-files-found: error
          retention-days: 30
