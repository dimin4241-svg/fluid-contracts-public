name: fSL feeOrReward canonical check 20260809

on:
  push:
    paths:
      - '.github/workflows/fsl-fee-watch-20260809-0155.yml'

permissions:
  contents: write

# automation-trigger: 2026-08-08T22:52:36Z
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit/read-only-dex-fees-20260805
      - name: Run all 21 canonical Routescan checks with RPC fallback
        shell: bash
        run: |
          python3 -m pip install -q requests
          set +e
          python3 scripts/security/check_fsl_fee_or_reward.py > scripts/security/fsl_last_result.json
          rc=$?
          echo "checker_exit_code=$rc"
          cat scripts/security/fsl_last_result.json
          exit 0
      - name: Commit result
        shell: bash
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add scripts/security/fsl_last_result.json
          git commit -m 'Record fresh fSL feeOrReward canonical check' || exit 0
          git push origin HEAD:audit/read-only-dex-fees-20260805
