[profile.default]
auto_detect_solc = true
evm_version = "cancun"
src = "src"
test = "test"
out = "out"
cache_path = "cache"
libs = ["../../lib"]
remappings = ["forge-std/=../../lib/forge-std/src/"]
optimizer = true
optimizer_runs = 200
fs_permissions = [{ access = "read-write", path = "./" }]
